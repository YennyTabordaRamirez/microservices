server:
    port:8081
## mapeo de los archivos de configuraciòn de los microservicios

spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/YennyTabordaRamirez/microservices.git
                    default-label: develop
                    searchPaths: config-data
                    username: ${GIT_USER}
                    password: ${GIT_PASSWORD}
    security:
        user:
            name: root
            password: s3cr3t